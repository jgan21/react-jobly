import React from "react";

/**JobCard: display info about a single job
 *
 * State:
 * -None
 *
 * Props:
 * -jobData: an object of job data {id, title, salary, equity, companyHandle}
 *
 * JobCardList -> JobCard
 */

function JobCard({ jobData }){

}

export default JobCard;